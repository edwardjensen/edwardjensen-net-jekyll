name = "maps-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# The API key should be set as a secret:
# wrangler secret put GOOGLE_MAPS_API_KEY

# Routes configuration - update these for your domain
# Uncomment and configure when ready to deploy:
# routes = [
#   { pattern = "maps.edwardjensen.net/*", zone_name = "edwardjensen.net" }
# ]
