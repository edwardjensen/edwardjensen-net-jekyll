# Environment variables for cache refresh operations
# Copy this file to .env and fill in your values
#
# NOTE: These variables are NOT needed for local Jekyll development.
# The Jekyll plugin reads the GraphQL endpoint from _config.yml (or _config.local.yml).
# The GRAPHQL_ENDPOINT env var is only used by CI workflows to override the config.

# ============================================================================
# Cache Refresh Script (npm run cache:refresh)
# ============================================================================
# These are needed for running the cache refresh script locally.
# The script fetches from CMS via Tailscale and writes to the Cloudflare cache.

# API key for writing to the Cloudflare GraphQL cache worker
CACHE_API_KEY=your-cache-api-key-here
