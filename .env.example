# Environment variables for local development
# Copy this file to .env and fill in your values

# ============================================================================
# GraphQL Endpoint
# ============================================================================
# The Jekyll plugin fetches CMS content from this GraphQL endpoint.
# Both the Cloudflare worker and direct CMS use the same /api/graphql path.

# Option 1: Use the public GraphQL endpoint (default, no VPN required)
GRAPHQL_ENDPOINT=https://graphql.edwardjensen.net/api/graphql

# Option 2: Use the CMS directly via Tailscale VPN
# Uncomment this line and comment out the above to work with unpublished content.
# GRAPHQL_ENDPOINT=https://www-ts.edwardjensencms.com/api/graphql

# ============================================================================
# Cache Refresh (not needed for local development)
# ============================================================================
# These are only needed if you're testing cache refresh operations locally.
# CACHE_API_KEY=your-cache-api-key-here
